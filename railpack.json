{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn chataccount.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}
